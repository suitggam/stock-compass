📌 오늘 학습한 내용 요약

 기획
빅데이터 분산 공부
ERD 및 기획 최종

🧠 상세 내용 정리

1. 한 일

ERD 최종


🤔 느낀 점 & 회고

ERD 최종
기획 최종


📝 상세하게 공부한 점 
저장 (HDFS/S3)
처리 (Spark/Flink)
쿼리 (Hive/Trino)
파이프라인 (Kafka/Airflow)
거버넌스/보안 (Ranger/Atlas)
응용 (ML, 검색, 그래프)

1.hive: HDFS위에 sql 인터페이스 제공----sql-mapreuce로 번역해주는 레이어 역할
2. SpqrkSQL: DATAframe으로 sql테이블 제공 ///SQL+dataframe API 같이 씀 
          spark+hive조합으로 기본세트
        이중 sparksql을 많이 씀 
왜?GPT가 아래처럼 대답해줌

속도
Hive는 원래 MapReduce 위에서 돌았음 → 느림.
Spark SQL은 Catalyst Optimizer라는 쿼리 최적화 엔진 + 메모리 기반 처리 → 훨씬 빠름.

호환성
Hive 메타스토어 연결 가능 → Hive 테이블 그대로 Spark SQL에서 쓸 수 있음.
즉, Hive에서 정의한 테이블 스키마 그대로 Spark SQL에서 조회 가능.

개발 편의성
SQL 문법 + DataFrame API(코드형) 둘 다 사용 가능.
예: df.filter(df.age > 30) vs spark.sql("SELECT * FROM people WHERE age > 30")